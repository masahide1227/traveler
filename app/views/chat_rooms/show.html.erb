<div class="chat-room">
  <h1>チャットルーム: <%= @chat_room.name %></h1>

  <div class="messages">
    <% @chat_room.messages.each do |message| %>
      <div class="message">
        <p><strong><%= message.user.name %>:</strong> <%= message.body %></p>
      </div>
    <% end %>
  </div>

  <div class="new-message">
    <%= form_with(model: [@chat_room, @chat_room.messages.build], local: true) do |form| %>
      <div class="form-group">
        <%= form.label :body, "メッセージ" %>
        <%= form.text_area :body, rows: 3, class: "form-control", maxlength: 140 %>
      </div>
      <%= form.submit "送信", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>


